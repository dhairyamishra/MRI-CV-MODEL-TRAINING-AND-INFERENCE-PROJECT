# Platform-specific overrides
# Automatically applied based on detected OS

platforms:
  windows:
    training:
      num_workers: 0
      pin_memory: false
  
  linux:
    training:
      num_workers: 4
      pin_memory: true
  
  mac:
    training:
      num_workers: 2
      pin_memory: true
